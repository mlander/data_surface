# Two ways for an agent to create a content type

Generated by `scripts/generate-comparison.php`; do not edit by hand.
`NodeTypeToolComparisonTest` asserts on every run that this file still
matches what that script produces, and every cell of the table below is
what the test observed, not what anybody expected.

`data_surface_demo_extras` adds two editorial review settings to every
content type, and adds them twice: once to the content type surface
through the build event, and once to core's own content type form
through a form alter, written the way core's own modules write one. Both
store the same third party settings, under the same config schema, with
the same rules:

- `review_deadline`, an integer of seconds from 3600 to 2592000 — one
  hour to thirty days — or nothing. The config schema says all of that
  except the unit: it declares an integer with a Range of 3600 to
  2592000, and the key is not named for what it counts. Core's form asks
  a person for an amount and a unit, hours, days, weeks or business
  days, and turns the pair into seconds in its element validator. The
  surface asks for the same amount and unit, and the same conversion is
  a storage shape the extras module hands the surface, which the target
  applies when it prepares what it writes. Business days follow one
  rule on both sides: counted from the start of a Monday, N business
  days of 24 hours span `N + 2 * floor((N - 1) / 5)` calendar days, so
  ten are twelve days, 1036800 seconds.
- `audience_tags`, a list of lower case tags, each once. Core's form
  takes them as comma-separated text and splits, trims, lower cases and
  de-duplicates what a person typed; the schema says only that it is a
  list of strings.

So the classic side has an accurate, complete schema for the deadline,
and the gap it leaves is not validation. It is meaning: what the stored
integer counts, and how what a person says becomes it, live only in form
code, so an agent reading the schema alone has to infer them. A capable
agent will read 3600 and 2592000 as an hour and thirty days in seconds
and guess the unit right; the table records that it does. The claim is
not that it cannot, but that a correct guess is still unverified
inference, found right or wrong only after the value is stored, while
the surface's caller never infers, because the unit is part of the
contract. Business days are the case no reading of the schema can
answer: the schema-only agent's best inference is in range, accepted,
and wrong.

Neither tool below was written with those settings in mind.
`data_surface:node_type_add` names no key of a content type at all: its
one values input is the content type surface, converted when the tool is
asked for its inputs. `tool_belt:entity_bundle_add` is what an agent has
for creating a content type without surfaces. It derives its
`properties` input from the node type's config schema and the labels on
core's form, and it leaves `third_party_settings` out of what it
advertises on purpose.

Both schemas below were produced by the Tool API's own definition
serializer, which is the document an MCP client or a function calling
model is handed.

## What happens

Each case is sent to each tool as the value its schema would carry — for
the classic tool, as the stored schema reads, inside the
`third_party_settings` map the tool does not advertise, which is the
only way to reach the settings at all — and typed into core's form as a
person would type it. "Stored" is the content type's
`data_surface_demo_extras` third party settings after the call. Where a
stored value breaks the stored schema, the cell says so: the classic
tool writes without asking it, as core's config save does on any site.
The surface refuses tags it would have to rewrite rather than rewriting
them: the pipeline never turns a value into a different one on a
caller's behalf, so the contract says what a stored tag looks like and a
caller sends that.

| Case | `data_surface:node_type_add` | `tool_belt:entity_bundle_add` | Core's content type form, for a person |
| --- | --- | --- | --- |
| One week. Surface: `{"amount": 1, "unit": "weeks"}`; classic: `604800`, by a schema-only agent that reads 3600 and 2592000 as an hour and thirty days in seconds and infers the unit, rightly, though nothing tells it so before storage; form: 1, Weeks | Created. Stored `{"review_deadline":604800,"audience_tags":[]}`. | Created. Stored `{"review_deadline":604800}`. | Stored `{"review_deadline":604800,"audience_tags":[]}`. |
| Forty-five days, past the ceiling. Surface: `{"amount": 45, "unit": "days"}`; classic: `3888000`; form: 45, Days | Refused; nothing created. `third_party_settings.data_surface_demo_extras.review_deadline.amount`: A review deadline is at least one hour and at most thirty days; 45 days is outside that. | Created. Stored `{"review_deadline":3888000}`. The stored schema refuses it (This value should be between 3600 and 2592000.), but nothing on the write path asked. | Refused: The review deadline must be between one hour and thirty days. |
| A schema-only agent that infers instead that the integer counts days sends `7`, meaning seven. Form: 7, Days, which is what it meant | Refused; nothing created. `third_party_settings.data_surface_demo_extras.review_deadline`: The Review deadline input is invalid: Invalid values given. Values must be represented as an associative array. | Created. Stored `{"review_deadline":7}`. The stored schema refuses it (This value should be between 3600 and 2592000.), but nothing on the write path asked. | Stored `{"review_deadline":604800,"audience_tags":[]}`. |
| Ten business days. Surface: `{"amount": 10, "unit": "business_days"}`; classic: `864000`, a schema-only agent's best inference, ten days in seconds: plausible, in range, and wrong, since no reading of the schema says what a business day becomes; form: 10, Business days | Created. Stored `{"review_deadline":1036800,"audience_tags":[]}`. | Created. Stored `{"review_deadline":864000}`. | Stored `{"review_deadline":1036800,"audience_tags":[]}`. |
| Set the tags `["news", "sports"]`. Form: news, sports | Created. Stored `{"review_deadline":null,"audience_tags":["news","sports"]}`. | Created. Stored `{"audience_tags":["news","sports"]}`. | Stored `{"review_deadline":null,"audience_tags":["news","sports"]}`. |
| Tags as a person types them: `["News", "news ", "Sports"]`. Form: News, news , Sports | Refused; nothing created. `third_party_settings.data_surface_demo_extras.audience_tags.0`, `third_party_settings.data_surface_demo_extras.audience_tags.1`, `third_party_settings.data_surface_demo_extras.audience_tags.2`: An audience tag is lower case letters and digits, words joined by one space or one hyphen, with nothing around it. | Created. Stored `{"audience_tags":["News","news ","Sports"]}`. | Stored `{"review_deadline":null,"audience_tags":["news","sports"]}`. |
| Unsplit tags: `"News, Sports"`. Form: the same text | Refused; nothing created. `third_party_settings.data_surface_demo_extras.audience_tags.0`: An audience tag is lower case letters and digits, words joined by one space or one hyphen, with nothing around it. | Created. Stored `{"audience_tags":"News, Sports"}`. Not the type the stored schema declares. | Stored `{"review_deadline":null,"audience_tags":["news","sports"]}`. |
| Dry run of one week | Accepted and prepared; nothing created. | No such input: The dry_run context is not a valid context. | No equivalent. |

## What an agent is handed: `data_surface:node_type_add`

```json
{
    "type": "object",
    "properties": {
        "values": {
            "type": "object",
            "properties": {
                "name": {
                    "type": "string",
                    "maxLength": 255,
                    "description": "Name: The human readable name for this content type.",
                    "title": "Name"
                },
                "type": {
                    "type": "string",
                    "maxLength": 32,
                    "pattern": "^[a-z0-9_]+$",
                    "description": "Machine name: Unique machine readable name: lowercase letters, numbers, and underscores only.",
                    "title": "Machine name"
                },
                "description": {
                    "oneOf": [
                        {
                            "type": "string"
                        },
                        {
                            "type": "null"
                        }
                    ],
                    "description": "Description: Displays on the Content types page.",
                    "title": "Description"
                },
                "title_label": {
                    "type": "string",
                    "default": "Title",
                    "maxLength": 255,
                    "description": "Title field label: The label shown for the title field on the content form.",
                    "title": "Title field label"
                },
                "preview_mode": {
                    "type": "integer",
                    "default": 1,
                    "enum": [
                        0,
                        1,
                        2
                    ],
                    "description": "Preview before submitting",
                    "title": "Preview before submitting"
                },
                "help": {
                    "oneOf": [
                        {
                            "type": "string"
                        },
                        {
                            "type": "null"
                        }
                    ],
                    "description": "Explanation or submission guidelines: Displayed at the top of the page when creating or editing content of this type.",
                    "title": "Explanation or submission guidelines"
                },
                "status": {
                    "oneOf": [
                        {
                            "type": "boolean"
                        },
                        {
                            "type": "null"
                        }
                    ],
                    "default": true,
                    "description": "Published: Whether new content of this type is published by default.",
                    "title": "Published"
                },
                "promote": {
                    "oneOf": [
                        {
                            "type": "boolean"
                        },
                        {
                            "type": "null"
                        }
                    ],
                    "description": "Promoted to front page: Whether new content of this type is promoted by default.",
                    "title": "Promoted to front page"
                },
                "sticky": {
                    "oneOf": [
                        {
                            "type": "boolean"
                        },
                        {
                            "type": "null"
                        }
                    ],
                    "description": "Sticky at top of lists: Whether new content of this type is sticky by default.",
                    "title": "Sticky at top of lists"
                },
                "new_revision": {
                    "oneOf": [
                        {
                            "type": "boolean"
                        },
                        {
                            "type": "null"
                        }
                    ],
                    "default": true,
                    "description": "Create new revision: Whether edits create a new revision by default.",
                    "title": "Create new revision"
                },
                "display_submitted": {
                    "oneOf": [
                        {
                            "type": "boolean"
                        },
                        {
                            "type": "null"
                        }
                    ],
                    "default": true,
                    "description": "Display author and date information: Author username and publish date will be displayed.",
                    "title": "Display author and date information"
                },
                "third_party_settings": {
                    "type": [
                        "object",
                        "null"
                    ],
                    "properties": {
                        "data_surface_demo_extras": {
                            "type": [
                                "object",
                                "null"
                            ],
                            "properties": {
                                "review_deadline": {
                                    "type": [
                                        "object",
                                        "null"
                                    ],
                                    "properties": {
                                        "amount": {
                                            "oneOf": [
                                                {
                                                    "type": "integer"
                                                },
                                                {
                                                    "type": "null"
                                                }
                                            ],
                                            "minimum": 1,
                                            "description": "Amount",
                                            "title": "Amount"
                                        },
                                        "unit": {
                                            "oneOf": [
                                                {
                                                    "type": "string"
                                                },
                                                {
                                                    "type": "null"
                                                }
                                            ],
                                            "default": "days",
                                            "enum": [
                                                "hours",
                                                "days",
                                                "weeks",
                                                "business_days"
                                            ],
                                            "description": "Unit",
                                            "title": "Unit"
                                        }
                                    },
                                    "description": "Review deadline: How long an editor has to review new content of this type, from one hour to thirty days. Leave the amount empty for no deadline.",
                                    "title": "Review deadline"
                                },
                                "audience_tags": {
                                    "type": [
                                        "array",
                                        "null"
                                    ],
                                    "description": "Audience tags: Who content of this type is written for, each tag listed once and in lower case, for example \"local news\" or \"sports\".",
                                    "items": {
                                        "oneOf": [
                                            {
                                                "type": "string"
                                            },
                                            {
                                                "type": "null"
                                            }
                                        ],
                                        "pattern": "^[a-z0-9]+(?:[ -][a-z0-9]+)*$",
                                        "description": "Audience tag",
                                        "title": "Audience tag"
                                    },
                                    "title": "Audience tags"
                                }
                            },
                            "description": "Data Surface Demo Extras settings",
                            "title": "Data Surface Demo Extras settings"
                        }
                    },
                    "description": "Third party settings: Settings added by other modules.",
                    "title": "Third party settings"
                }
            },
            "required": [
                "name",
                "type"
            ],
            "description": "Content type values: The settings of the new content type, as the content type surface describes them.",
            "title": "Content type values"
        },
        "dry_run": {
            "oneOf": [
                {
                    "type": "boolean"
                },
                {
                    "type": "null"
                }
            ],
            "description": "Dry run: Accept, validate and prepare the values without creating anything.",
            "title": "Dry run"
        }
    },
    "additionalProperties": false,
    "required": [
        "values"
    ]
}
```

## What an agent is handed: `tool_belt:entity_bundle_add`, for `node`

```json
{
    "type": "object",
    "properties": {
        "entity_type_id": {
            "type": "string",
            "description": "Entity Type ID: The machine name of the entity type to add a bundle for.",
            "title": "Entity Type ID"
        },
        "bundle": {
            "type": "string",
            "description": "Bundle ID: The machine name of the bundle to create.",
            "title": "Bundle ID"
        },
        "label": {
            "type": "string",
            "description": "Label: The human-readable label for the bundle.",
            "title": "Label"
        },
        "properties": {
            "type": [
                "object",
                "null"
            ],
            "properties": {
                "langcode": {
                    "oneOf": [
                        {
                            "type": "string"
                        },
                        {
                            "type": "null"
                        }
                    ],
                    "description": "Language code: The input definition for Language code",
                    "title": "Language code"
                },
                "status": {
                    "oneOf": [
                        {
                            "type": "boolean"
                        },
                        {
                            "type": "null"
                        }
                    ],
                    "default": "status",
                    "description": "Published: The input definition for Status",
                    "title": "Published"
                },
                "description": {
                    "oneOf": [
                        {
                            "type": "string"
                        },
                        {
                            "type": "null"
                        }
                    ],
                    "description": "Description: Displays on the <em>Content types</em> page.",
                    "title": "Description"
                },
                "help": {
                    "oneOf": [
                        {
                            "type": "string"
                        },
                        {
                            "type": "null"
                        }
                    ],
                    "description": "Explanation or submission guidelines: This text will be displayed at the top of the page when creating or editing content of this type.",
                    "title": "Explanation or submission guidelines"
                },
                "new_revision": {
                    "oneOf": [
                        {
                            "type": "boolean"
                        },
                        {
                            "type": "null"
                        }
                    ],
                    "description": "Whether a new revision should be created by default: The input definition for Whether a new revision should be created by default",
                    "title": "Whether a new revision should be created by default"
                },
                "preview_mode": {
                    "oneOf": [
                        {
                            "type": "integer"
                        },
                        {
                            "type": "null"
                        }
                    ],
                    "default": 1,
                    "enum": [
                        0,
                        1,
                        2
                    ],
                    "description": "Preview before submitting: The input definition for Preview before submitting. Allowed values: {\"0\":\"Disabled\",\"1\":\"Optional\",\"2\":\"Required\"}",
                    "title": "Preview before submitting"
                },
                "display_submitted": {
                    "oneOf": [
                        {
                            "type": "boolean"
                        },
                        {
                            "type": "null"
                        }
                    ],
                    "default": true,
                    "description": "Display author and date information: Author username and publish date will be displayed.",
                    "title": "Display author and date information"
                }
            },
            "description": "Properties: Additional properties to set on the bundle configuration entity.",
            "title": "Properties"
        }
    },
    "additionalProperties": false,
    "required": [
        "entity_type_id",
        "bundle",
        "label"
    ]
}
```
