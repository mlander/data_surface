services:
  data_surface_demo_node_type.provider:
    class: Drupal\data_surface_demo_node_type\NodeTypeSurfaceProvider
    arguments:
      - '@data_surface.factory'
      - '@entity_type.manager'
      - '@entity_field.manager'
      - '@config.typed'
      - '@current_user'
      - '@string_translation'
  # The hook class is autowired by core's hook discovery, so the provider
  # it depends on needs a service to resolve its type hint against.
  Drupal\data_surface_demo_node_type\NodeTypeSurfaceProvider: '@data_surface_demo_node_type.provider'
  # The cosmetic layer the two routes name beside the provider: the
  # vertical tabs, the machine name mirror, the message and the redirect,
  # which is all a form class is still for here.
  data_surface_demo_node_type.form_cosmetics:
    class: Drupal\data_surface_demo_node_type\Form\NodeTypeSurfaceFormCosmetics
    arguments: ['@string_translation']
